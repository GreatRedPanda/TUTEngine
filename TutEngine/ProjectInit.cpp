#include "ProjectInit.h"

using namespace std::filesystem;

ProjectInit::ProjectInit()
{
	//std::filesystem::path path{ "D:/TutProject/Assets" }; //creates TestingFolder object on C:
	////path /= "SampleStage.stg"; //put something into there
	////std::filesystem::create_directories(path.parent_path()); //add directories based on the object path (without this line it will not work)
	//if (!std::filesystem::exists(path))
	//{
	//	create_directories("D:/TutProject/Assets");
	//}
	//path /= "SampleStage.stg"; //put something into there

	//if (!std::filesystem::exists(path))
	//{
	//	//std::ofstream of1();
	//	//of1.close();
	//	std::ofstream ofs= std::ofstream(path);
	//	ofs.close();
	//}
	////std::ofstream ofs(path);	
	////ofs.close();
}
